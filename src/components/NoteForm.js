import React from 'react';
import PropTypes from 'prop-types';

function NoteForm({ onSubmit, onChange, title, body, submitText }){
  return (
    <form>
      
    </form>
  );
}

//react way of setting default props
NoteForm.defaultProps = {
  submitText: 'Submit'
};


